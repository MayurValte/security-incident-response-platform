logging:
  level:
    root: INFO

server:
  shutdown: graceful

spring:

  application:
    name: notification-service

  config:
    import: configserver:http://localhost:8888

  cloud:

    config:
      fail-fast: true
