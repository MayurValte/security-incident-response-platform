spring:
  application:
    name: user-service

  config:
    import: configserver:http://localhost:8888

  cloud:
    config:
      fail-fast: true

# PUBLIC key only - this service verifies tokens, never issues them.
jwt:
  public-key: ${JWT_PUBLIC_KEY}
